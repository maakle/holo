---
status: ACTIVE
---
# CEO Plan: holo v0.1 — YC-pitch-ready expansion

Generated by /plan-ceo-review on 2026-04-29
Branch: main | Mode: SELECTIVE EXPANSION
Repo: maakle/holo

## Vision

### 10x Check
The 10x version of holo doesn't ship more connectors. It ships **network effects on procedural knowledge**. Every team that adopts holo contributes anonymized skills (how their team handles X) into a shared registry. The first 100 published skills become the marketing growth loop — "How do top engineering teams handle GDPR escalations?" becomes a queryable corpus that brings the next 1000 teams in.

Beyond the marketplace: **holo graduates from middleware to operating system.** The "OS" framing isn't aspirational copy — it's a UI surface (agent observability dashboard + replay) that turns holo from "a thing my agent calls" into "a place my team goes when an agent acts weird." That's the difference between Stripe (infra) and Vercel (platform); same code, different shape.

The 10x version isn't more features. It's the same product positioned for compounding adoption.

### Premise reframes (D34/D35/D36)

Three framing decisions this session that compound the YC pitch without expanding build scope:

- **P1 (D34): Add explicit "managed cloud coming" framing** to README/VISION. OSS-self-host stays primary, but the path to a $1B company is signalled. Pattern: Supabase, Linear (open-source CLI). Same code, different business framing.
- **P2 (D35): "Layer today, OS tomorrow" framing.** Honors Hu's RFS "AI Operating System for Companies" by name. Doesn't lock in OS-level engineering scope yet; reframes the trajectory.
- **P3 (D36): Land a verbal design-partner commitment in week 1.** Don't change v0.0's internal-dogfood scope. Do pick up the phone with a peer CTO this week and get a "if holo existed and worked, would you swap your support agent's bespoke retriever for it in 4 weeks?" Yes-or-no answer. If yes, you have a logo for the YC pitch *during* v0.0.

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| CP1 | Skill marketplace stub in v0.1 | ~5–7 days (revised after spec review) | ACCEPTED | YC pitch graduates from middleware to platform; first 100 published skills become marketing |
| CP2 | Agent observability dashboard + replay (read-only side-by-side) in v0.1 | ~1 week | ACCEPTED | Makes "OS tomorrow" framing concrete; replay v1 is recorded query+result diff (live re-execution deferred to v0.2 to avoid mutating-tool risk) |
| CP3 | `npx holo init` single-line install in v0.1 (macOS + Linux only) | ~2 days | ACCEPTED | Drops time-to-hello-world from ~10 min to ~2 min; differentiates DX vs Onyx/Dust |
| CP4 | Productize 3 v0.0 agents as YAML templates | ~3 days | DEFERRED | User chose to revisit post-v0.1 once external-CTO demand surfaces |
| CP5 | Inferred org chart / who-does-what | ~1 week | NOT SURFACED | Lower priority; available for future review |
| CP6 | Slack `/holo` slash command | ~3–4 days | NOT SURFACED | Lower priority; alternate-audience play |
| CP7 | BYO-LLM config UI | ~2 days | NOT SURFACED | Lower priority; current env-var approach acceptable |
| CP8 | Skill eval mode (golden inputs/outputs) | ~1 week | NOT SURFACED | Builds on v0.1 eval harness; v0.2 candidate |
| CP9 | Anonymized public benchmarks | ~1 week | NOT SURFACED | Privacy-sensitive; v0.2+ |

## Accepted Scope (added to v0.1)

v0.1 timeline shifts from 7–8 weeks to **~10–11 weeks** to absorb the three accepted CPs (~2.5 weeks of marginal work) plus 1 week of buffer for integration/QA across three new surfaces. Without buffer the timeline is brittle; with it, the timeline is honest:

- **Skill marketplace stub** — `Publish anonymized` button on extracted skills + public registry page (`/skills` route in `apps/web`). **Two-stage publish:** automated LLM-redaction pass produces a diff; human reviews and confirms before anything goes public. GitHub OAuth required to publish (browse is anonymous). AGPL-3.0 contribution license (marketplace publishes are Community Edition). Takedown email + rate limit on publish (5/day per user). **Skill = YAML doc + redacted example outputs.**
- **Agent observability dashboard + replay (read-only)** — page in `apps/web` showing last 100 agent invocations with: timestamp, calling agent identity, tools called, latency, output preview. **Replay v1 = side-by-side diff of the recorded query and result; no live re-execution.** Live re-execution deferred to v0.2 once tool-effect classification (read-only vs side-effecting) is in place. Requires every MCP call to log query+result (already implied for v0.1 observability).
- **`npx holo init` package** — `create-holo` npm package, **macOS + Linux only for v0.1** (Windows in v0.2). Scaffolds a holo install: download, generate `.env` with safe defaults, prompt for required values (LLM provider key), run `docker compose up`, run migrations, open the Connections page. Handles "docker daemon not running" gracefully with a clear error message.

## Deferred to TODOS.md

- **CP4 — Agent templates:** productize the 3 v0.0 agents (support-question, interview-prep, customer-success) as YAML templates in `packages/agent-templates/`. Build once 3+ external CTOs are using holo and ask. Reasoning: keeps v0.1 scope tight; templates are most valuable when there's external demand to reuse them.

## Success Metrics (v0.1)

Numeric targets that gate "v0.1 succeeded" beyond the binary success criteria already in the design doc:

- **Time-to-hello-world** (`npx holo init` to first `search` query): **≤ 3 minutes** measured on a clean Linux VM and a clean macOS environment.
- **Marketplace publishes** (cumulative across external CTOs in first 30 days post-launch): **≥ 5 published skills**, all clean of named entities (founder reviews each manually for v0.1).
- **Replay-page usage**: **≥ 1 view per active CTO per week** in the first month — proxy for "OS tomorrow" framing landing.
- **Design-partner verbal commit**: **landed within 2 weeks** of v0.0 build start (P3/D36). If not landed by week 2, escalate scope decision.

## Risks & mitigations

- **LLM-redaction misses PII** in skill marketplace publish → leaks customer names. Mitigation: two-stage publish with explicit human confirmation diff; allowlist-only fields for v0.1; takedown SLA (24h).
- **Replay live re-execution would re-trigger side-effecting MCP tools.** Mitigation: replay v1 is read-only diff; live re-execution gated on per-tool effect classification (v0.2).
- **`npx holo init` Windows fragility.** Mitigation: explicit macOS + Linux scope for v0.1; document Windows as v0.2.
- **Design-partner verbal yes within 1 week assumes peer-CTO availability.** Mitigation: 2-week window with a named target list of 5 CTOs (founder owns); decision rule: if 0 yeses by week 2, document why and proceed with internal-only v0.0.

## Pitch sharpening (output of this review)

The combined effect of D34/35/36 + the 3 accepted CPs is a meaningfully stronger YC application:

- **Headline:** "holo — the open-source company-brain for AI agents. Layer today, OS tomorrow."
- **Pitch:** "Engineering teams are building 2+ custom AI agents in production today, each with bespoke context-fetching code. Holo is the shared MCP context layer that the agents your team is already shipping all point at. Today: context layer + procedural skills. Tomorrow: agent operating system. Deploy via `npx holo init` or self-host — Community Edition is AGPL-3.0, Enterprise Edition (SSO, RBAC, query history, whitelabeling) is paid; managed cloud later."
- **Differentiation:** "Onyx and Dust own search; Interloom and Potpie are enterprise-only. Holo is the only OSS-self-hostable layer that ships a skill marketplace from day one and is built for teams running 2+ agents already."
- **Demo:** "Here are 5 procedures holo extracted from one engineering team's last quarter of work. Here's an external Cursor agent invoking one of them via MCP. And here's the replay button showing exactly what context grounded the answer."

This is the difference between "interesting middleware" and "ambitious platform." Same code in v0.1; different framing + 3 small features.

## External validation signals

Running list of public datapoints from operators running coding agents at scale that confirm the theses underneath holo's three pillars. Update as new signals land.

### 2026-05-11 — Tobi Lütke on Shopify's River (Slack-native coding agent)

Source: https://x.com/tobi/status/2053121182044451016

What he says, in his numbers: River authored **1,870 PRs in the last week** in Shopify's main monorepo, and roughly **1 in 8 merged PRs** last week was authored by River and reviewed by humans. Across the last 30 days, **5,938 employees** worked with River in 4,450 Slack channels.

The merge rate went from **36% → 77% over two months**. He's explicit about the cause:

> "We did not retrain a model. We did not switch models. An improvement from 36% to 77% over two months came from people watching River work, noticing where it got stuck, and writing down what it should have known and helping make River itself a better teammate. Every team's accumulated taste flows into the agent."

**This is the skills/procedures pillar.** It is the strongest public datapoint we have so far that the lever on coding-agent quality at company scale is *procedural knowledge captured from how the team actually works* — not the model, not the retriever, not the prompt. The 2x merge-rate gain over two months is exactly the curve [ADR-0003](../decisions/0003-skills-on-top-of-context-layer.md) bets on.

Secondary validations from the same post, in priority order:

- **Observability is the growth flywheel, not the compliance tax.** River only operates in public Slack channels — DMs are refused. "Every conversation is therefore searchable. Anyone at Shopify can jump in… People started learning from each other." This is the same mechanic CP2 (read-only replay) ships in v0.1, reframed: replay isn't just for audit, it's the substrate the team uses to *write down what the agent should have known*. The governance pillar is what makes the procedures pillar compound.
- **Per-team scoped context is required, not optional.** "Every channel can pre-load the zones, skills, and instructions its team needs, written by the people closest to the work." This is the scoped-personas + per-skill allowlist model on the v0.2 roadmap, validated as table-stakes for an agent that lands in production.
- **The agent needs unified access to everything the company writes.** River reads code, runs tests, opens PRs, queries the data warehouse, looks at prod traces, lives in Slack. The exact context-duplication pain [ADR-0004](../decisions/0004-multi-agent-shared-context-wedge.md) names — Shopify solved it by building the connectors themselves into one agent. The holo bet is that the next 1000 teams will not.
- **Memory matters.** "River also has a memory that is constantly learning and un-learning critical information about the company and the best way to do work." Aligns with the procedural-extraction direction in `packages/skills` (skills stay current as procedures evolve).

**Honest caveat — what this does *not* validate.** River *is* the agent; holo says "bring your own agent." So the post validates the mechanic (context + procedures + observability compound to 2x agent quality at company scale) but not the positioning (shared MCP layer vs. one company-built agent). The counter-argument it implicitly raises: companies with the engineering muscle to build their own River may not need a layer underneath it. The bet is that the long tail — the next 100 engineering teams that want a River — cannot afford to write their own, and that's the market holo serves. Worth pressure-testing in cold-DM conversations: ask peer CTOs whether they'd rather build their own River or point a Cursor/Claude agent at a holo instance with the same context surface.

**For the YC pitch:** this is the strongest external proof point we have that the procedures pillar is the right wedge — and it lands as a quantitative number (2x merge rate, two months, same model) from a credibly-known operator. Lead with it.
