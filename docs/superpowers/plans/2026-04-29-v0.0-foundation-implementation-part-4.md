# holo v0.0 Foundation — Implementation Plan, Part 4: API, MCP, Worker apps

> Continues from [Part 3](./2026-04-29-v0.0-foundation-implementation-part-3.md). Run that first.

**Phases covered:**
- Phase 5 — `apps/api` (NestJS REST app: `/health`, AuthGuard, error filter)
- Phase 6 — `apps/mcp` (Hono service: `/health`, cookie-session middleware wired but ungated)
- Phase 7 — `apps/worker` (NestJS standalone with BullMQ heartbeat queue)

---

## Phase 5 — `apps/api`

### Task 5.1: NestJS scaffold + /health

**Files:**
- Create: `apps/api/package.json`
- Create: `apps/api/tsconfig.json`
- Create: `apps/api/nest-cli.json`
- Create: `apps/api/src/main.ts`
- Create: `apps/api/src/app.module.ts`
- Create: `apps/api/src/health.controller.ts`

- [ ] **Step 1: Create `apps/api/package.json`**

```json
{
  "name": "@holo/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@holo/auth": "workspace:*",
    "@holo/db": "workspace:*",
    "@holo/env": "workspace:*",
    "@holo/errors": "workspace:*",
    "@holo/retrieval-core": "workspace:*",
    "@nestjs/common": "11.0.0",
    "@nestjs/core": "11.0.0",
    "@nestjs/platform-express": "11.0.0",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.1",
    "pino": "9.5.0",
    "pino-pretty": "11.3.0",
    "nestjs-pino": "4.1.0"
  },
  "devDependencies": {
    "@nestjs/cli": "11.0.0",
    "@nestjs/testing": "11.0.0",
    "@types/node": "20.16.11",
    "supertest": "7.0.0",
    "@types/supertest": "6.0.2",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
```

- [ ] **Step 2: Create `apps/api/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "target": "ES2022",
    "isolatedModules": false
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Create `apps/api/nest-cli.json`**

```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": { "deleteOutDir": true }
}
```

- [ ] **Step 4: Create `apps/api/src/health.controller.ts`**

```ts
import { Controller, Get } from '@nestjs/common';

@Controller('health')
export class HealthController {
  @Get()
  health() {
    return { status: 'ok', service: 'api' };
  }
}
```

- [ ] **Step 5: Create `apps/api/src/app.module.ts`**

```ts
import { Module } from '@nestjs/common';
import { LoggerModule } from 'nestjs-pino';
import { HealthController } from './health.controller.js';

@Module({
  imports: [
    LoggerModule.forRoot({
      pinoHttp: {
        transport: process.env.NODE_ENV === 'production' ? undefined : { target: 'pino-pretty' },
      },
    }),
  ],
  controllers: [HealthController],
})
export class AppModule {}
```

- [ ] **Step 6: Create `apps/api/src/main.ts`**

```ts
import 'reflect-metadata';
import { NestFactory } from '@nestjs/core';
import { Logger } from 'nestjs-pino';
import { initCrypto } from '@holo/crypto';
import { parseEnv } from '@holo/env';
import { AppModule } from './app.module.js';

async function bootstrap() {
  parseEnv(process.env);
  await initCrypto();

  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  app.useLogger(app.get(Logger));
  await app.listen(4000);
  console.log('apps/api listening on :4000');
}

bootstrap().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 7: Add `@holo/crypto` to deps** (oversight — added to imports above)

```bash
pnpm add -F @holo/api @holo/crypto@workspace:*
```

- [ ] **Step 8: Verify it builds + typechecks**

```bash
pnpm install
pnpm -F @holo/api typecheck
pnpm -F @holo/api build
```

Expected: no errors. `dist/main.js` produced.

- [ ] **Step 9: Commit**

```bash
git add apps/api pnpm-lock.yaml
git commit -m "feat(api): nestjs scaffold with /health and pino logger"
```

---

### Task 5.2: AuthGuard + error filter

**Files:**
- Create: `apps/api/src/auth/auth.guard.ts`
- Create: `apps/api/src/auth/auth.module.ts`
- Create: `apps/api/src/errors/holo-error.filter.ts`
- Modify: `apps/api/src/app.module.ts`
- Create: `apps/api/test/auth-guard.test.ts`

- [ ] **Step 1: Create `apps/api/src/auth/auth.module.ts`**

```ts
import { Module, Global } from '@nestjs/common';
import { createDb, type DB } from '@holo/db';
import { createAuth, type Auth } from '@holo/auth';
import { schema } from '@holo/db';
import { sql } from 'drizzle-orm';
import { parseEnv } from '@holo/env';

const env = parseEnv(process.env);
const db: DB = createDb(env.DATABASE_URL);

let cachedAuth: Auth | null = null;

async function getAuth(): Promise<Auth> {
  if (cachedAuth) return cachedAuth;
  const orgs = await db
    .select({ id: schema.organization.id })
    .from(schema.organization)
    .where(sql`slug = 'default'`);
  if (!orgs[0]) throw new Error('default organization not seeded; run pnpm db:migrate');
  cachedAuth = createAuth({ db, env, defaultOrganizationId: orgs[0].id });
  return cachedAuth;
}

export const DB_TOKEN = Symbol('DB');
export const AUTH_TOKEN = Symbol('Auth');

@Global()
@Module({
  providers: [
    { provide: DB_TOKEN, useValue: db },
    { provide: AUTH_TOKEN, useFactory: () => getAuth() },
  ],
  exports: [DB_TOKEN, AUTH_TOKEN],
})
export class AuthModule {}
```

- [ ] **Step 2: Create `apps/api/src/auth/auth.guard.ts`**

```ts
import { CanActivate, ExecutionContext, Inject, Injectable } from '@nestjs/common';
import { holoError, ErrorCode } from '@holo/errors';
import type { Auth } from '@holo/auth';
import { AUTH_TOKEN } from './auth.module.js';

@Injectable()
export class AuthGuard implements CanActivate {
  constructor(@Inject(AUTH_TOKEN) private readonly auth: Auth) {}

  async canActivate(ctx: ExecutionContext): Promise<boolean> {
    const req = ctx.switchToHttp().getRequest();
    const session = await this.auth.api.getSession({ headers: req.headers as Headers });
    if (!session) {
      throw holoError({
        code: ErrorCode.HOLO_AUTH_NO_SESSION,
        problem: 'no Better Auth session on request',
        fix: 'Sign in at the dashboard URL.',
      });
    }
    req.user = { id: session.user.id, email: session.user.email };
    return true;
  }
}
```

- [ ] **Step 3: Create `apps/api/src/errors/holo-error.filter.ts`**

```ts
import { ArgumentsHost, Catch, ExceptionFilter, HttpStatus } from '@nestjs/common';
import { HoloError } from '@holo/errors';

const STATUS_MAP: Record<string, number> = {
  HOLO_AUTH_NO_SESSION: HttpStatus.UNAUTHORIZED,
  HOLO_OAUTH_EXCHANGE_FAILED: HttpStatus.BAD_REQUEST,
  HOLO_TOKEN_DECRYPT_FAILED: HttpStatus.INTERNAL_SERVER_ERROR,
  HOLO_DB_CONNECTION_FAILED: HttpStatus.SERVICE_UNAVAILABLE,
  HOLO_CONNECTOR_NOT_IMPLEMENTED: HttpStatus.NOT_IMPLEMENTED,
  HOLO_ENV_INVALID: HttpStatus.INTERNAL_SERVER_ERROR,
};

@Catch(HoloError)
export class HoloErrorFilter implements ExceptionFilter {
  catch(err: HoloError, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const res = ctx.getResponse();
    const status = STATUS_MAP[err.code] ?? HttpStatus.INTERNAL_SERVER_ERROR;
    res.status(status).json(err.toJSON());
  }
}
```

- [ ] **Step 4: Update `apps/api/src/app.module.ts` to register `AuthModule` and `HoloErrorFilter`**

```ts
import { Module } from '@nestjs/common';
import { APP_FILTER } from '@nestjs/core';
import { LoggerModule } from 'nestjs-pino';
import { HealthController } from './health.controller.js';
import { AuthModule } from './auth/auth.module.js';
import { HoloErrorFilter } from './errors/holo-error.filter.js';

@Module({
  imports: [
    LoggerModule.forRoot({
      pinoHttp: {
        transport: process.env.NODE_ENV === 'production' ? undefined : { target: 'pino-pretty' },
      },
    }),
    AuthModule,
  ],
  controllers: [HealthController],
  providers: [{ provide: APP_FILTER, useClass: HoloErrorFilter }],
})
export class AppModule {}
```

- [ ] **Step 5: Write integration test `apps/api/test/auth-guard.test.ts`** that asserts the filter renders HoloError as JSON with the expected status

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { Test } from '@nestjs/testing';
import { Controller, Get, UseGuards, INestApplication } from '@nestjs/common';
import { APP_FILTER } from '@nestjs/core';
import request from 'supertest';
import { AuthGuard } from '../src/auth/auth.guard.js';
import { AuthModule } from '../src/auth/auth.module.js';
import { HoloErrorFilter } from '../src/errors/holo-error.filter.js';

@Controller('protected')
@UseGuards(AuthGuard)
class ProtectedController {
  @Get() ok() {
    return { ok: true };
  }
}

let app: INestApplication;

beforeAll(async () => {
  const mod = await Test.createTestingModule({
    imports: [AuthModule],
    controllers: [ProtectedController],
    providers: [{ provide: APP_FILTER, useClass: HoloErrorFilter }],
  }).compile();
  app = mod.createNestApplication();
  await app.init();
});

afterAll(async () => {
  await app.close();
});

describe('AuthGuard + HoloErrorFilter', () => {
  it('returns 401 with HoloError JSON when no session cookie', async () => {
    const res = await request(app.getHttpServer()).get('/protected');
    expect(res.status).toBe(401);
    expect(res.body.code).toBe('HOLO_AUTH_NO_SESSION');
    expect(res.body.problem).toBeTruthy();
    expect(res.body.fix).toBeTruthy();
  });
});
```

- [ ] **Step 6: Run tests** (requires the migrated DB up — Better Auth's drizzle adapter touches DB at construct time)

```bash
docker compose up -d postgres
DATABASE_URL=postgresql://holo:holo@localhost:5432/holo \
  REDIS_URL=redis://localhost:6379 \
  HOLO_TOKEN_ENCRYPTION_KEY=$(openssl rand -base64 32) \
  BETTER_AUTH_SECRET=$(openssl rand -base64 32) \
  BETTER_AUTH_URL=http://localhost:3000 \
  GITHUB_LOGIN_CLIENT_ID=test GITHUB_LOGIN_CLIENT_SECRET=test \
  GITHUB_CONNECTOR_CLIENT_ID=test GITHUB_CONNECTOR_CLIENT_SECRET=test \
  pnpm -F @holo/api test
```

Expected: 1 test passes.

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/auth apps/api/src/errors apps/api/src/app.module.ts apps/api/test
git commit -m "feat(api): AuthGuard + HoloErrorFilter renders HOLO_AUTH_NO_SESSION as 401 JSON"
```

---

## Phase 6 — `apps/mcp`

### Task 6.1: Hono scaffold with /health and ungated session middleware

**Files:**
- Create: `apps/mcp/package.json`
- Create: `apps/mcp/tsconfig.json`
- Create: `apps/mcp/src/main.ts`
- Create: `apps/mcp/src/middleware/session.ts`
- Create: `apps/mcp/test/health.test.ts`
- Create: `apps/mcp/vitest.config.ts`

- [ ] **Step 1: Create `apps/mcp/package.json`**

```json
{
  "name": "@holo/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@holo/auth": "workspace:*",
    "@holo/db": "workspace:*",
    "@holo/env": "workspace:*",
    "@holo/errors": "workspace:*",
    "@holo/retrieval-core": "workspace:*",
    "hono": "4.6.7",
    "@hono/node-server": "1.13.2",
    "pino": "9.5.0"
  },
  "devDependencies": {
    "@types/node": "20.16.11",
    "tsx": "4.19.1",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
```

- [ ] **Step 2: Create `apps/mcp/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "./dist", "rootDir": "./src" },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Create `apps/mcp/src/middleware/session.ts`**

```ts
import type { MiddlewareHandler } from 'hono';
import { validateSessionCookie } from '@holo/auth/mcp-session';
import type { DB } from '@holo/db';

export interface McpSessionVars {
  user: { userId: string; organizationId: string; email: string };
}

export function createSessionMiddleware(db: DB): MiddlewareHandler<{ Variables: McpSessionVars }> {
  return async (c, next) => {
    const cookie = c.req.header('cookie');
    const session = await validateSessionCookie(db, cookie);
    c.set('user', session);
    await next();
  };
}
```

- [ ] **Step 4: Create `apps/mcp/src/main.ts`**

```ts
import { Hono } from 'hono';
import { serve } from '@hono/node-server';
import { initCrypto } from '@holo/crypto';
import { parseEnv } from '@holo/env';
import { createDb } from '@holo/db';
import { HoloError } from '@holo/errors';
import { createSessionMiddleware } from './middleware/session.js';

async function main() {
  const env = parseEnv(process.env);
  await initCrypto();
  const db = createDb(env.DATABASE_URL);

  const app = new Hono();

  app.onError((err, c) => {
    if (err instanceof HoloError) {
      const status =
        err.code === 'HOLO_AUTH_NO_SESSION' ? 401 :
        err.code === 'HOLO_CONNECTOR_NOT_IMPLEMENTED' ? 501 :
        500;
      return c.json(err.toJSON(), status);
    }
    console.error(err);
    return c.json({ code: 'HOLO_INTERNAL', problem: 'unexpected error', fix: 'check server logs' }, 500);
  });

  app.get('/health', (c) => c.json({ status: 'ok', service: 'mcp' }));

  // Session middleware exists and is testable, but no MCP endpoints exist in Foundation.
  // Spec #2 will mount the JSON-RPC handler under .use('/mcp/*', createSessionMiddleware(db))
  app.get('/_session-check', createSessionMiddleware(db), (c) =>
    c.json({ user: c.get('user' as never) }),
  );

  serve({ fetch: app.fetch, port: 8090 });
  console.log('apps/mcp listening on :8090');
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 5: Create `apps/mcp/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({ test: { include: ['test/**/*.test.ts'], testTimeout: 30000 } });
```

- [ ] **Step 6: Write test `apps/mcp/test/health.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { Hono } from 'hono';

describe('mcp /health', () => {
  it('returns ok', async () => {
    const app = new Hono();
    app.get('/health', (c) => c.json({ status: 'ok', service: 'mcp' }));
    const res = await app.request('/health');
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual({ status: 'ok', service: 'mcp' });
  });
});
```

- [ ] **Step 7: Add a session middleware integration test `apps/mcp/test/session-middleware.test.ts`** (DB required)

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { Hono } from 'hono';
import postgres from 'postgres';
import { drizzle } from 'drizzle-orm/postgres-js';
import { schema } from '@holo/db';
import { sql as drizzleSql } from 'drizzle-orm';
import { createSessionMiddleware } from '../src/middleware/session.js';
import { HoloError } from '@holo/errors';

const url = process.env.DATABASE_URL ?? 'postgresql://holo:holo@localhost:5432/holo';
let pg: ReturnType<typeof postgres>;
let db: ReturnType<typeof drizzle>;
let userId: string;

beforeAll(async () => {
  pg = postgres(url, { max: 1 });
  db = drizzle(pg, { schema });
  const orgs = await db.select({ id: schema.organization.id }).from(schema.organization).where(drizzleSql`slug='default'`);
  const inserted = await db.insert(schema.user).values({ email: 'mcp-mw@example.com', organizationId: orgs[0]!.id }).returning({ id: schema.user.id });
  userId = inserted[0]!.id;
  await db.insert(schema.session).values({ userId, token: 'mw-token', expiresAt: new Date(Date.now() + 3600_000) });
});

afterAll(async () => {
  await db.delete(schema.session).where(drizzleSql`token='mw-token'`);
  await db.delete(schema.user).where(drizzleSql`email='mcp-mw@example.com'`);
  await pg.end();
});

describe('createSessionMiddleware', () => {
  const app = new Hono().use('*', createSessionMiddleware(db as never)).get('/me', (c) => c.json(c.get('user' as never)));
  app.onError((err, c) => err instanceof HoloError ? c.json(err.toJSON(), 401) : c.json({}, 500));

  it('attaches user when cookie valid', async () => {
    const res = await app.request('/me', { headers: { cookie: 'better-auth.session_token=mw-token' } });
    expect(res.status).toBe(200);
    const body = await res.json() as { userId: string };
    expect(body.userId).toBe(userId);
  });

  it('returns 401 with HoloError JSON when cookie missing', async () => {
    const res = await app.request('/me');
    expect(res.status).toBe(401);
    expect((await res.json() as { code: string }).code).toBe('HOLO_AUTH_NO_SESSION');
  });
});
```

- [ ] **Step 8: Run tests**

```bash
pnpm install
DATABASE_URL=postgresql://holo:holo@localhost:5432/holo pnpm -F @holo/mcp test
```

Expected: 3 tests pass.

- [ ] **Step 9: Verify dev mode boots**

```bash
DATABASE_URL=... REDIS_URL=... ... pnpm -F @holo/mcp dev &
sleep 2
curl -s http://localhost:8090/health
```

Expected: `{"status":"ok","service":"mcp"}`. Then `kill %1`.

- [ ] **Step 10: Commit**

```bash
git add apps/mcp pnpm-lock.yaml
git commit -m "feat(mcp): hono /health + cookie session middleware (no tools yet)"
```

---

## Phase 7 — `apps/worker`

### Task 7.1: NestJS standalone with BullMQ heartbeat queue

**Files:**
- Create: `apps/worker/package.json`
- Create: `apps/worker/tsconfig.json`
- Create: `apps/worker/nest-cli.json`
- Create: `apps/worker/src/main.ts`
- Create: `apps/worker/src/app.module.ts`
- Create: `apps/worker/src/heartbeat/heartbeat.module.ts`
- Create: `apps/worker/src/heartbeat/heartbeat.processor.ts`
- Create: `apps/worker/src/heartbeat/heartbeat.scheduler.ts`
- Create: `apps/worker/test/heartbeat.test.ts`
- Create: `apps/worker/vitest.config.ts`

- [ ] **Step 1: Create `apps/worker/package.json`**

```json
{
  "name": "@holo/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@holo/env": "workspace:*",
    "@holo/errors": "workspace:*",
    "@nestjs/bullmq": "11.0.0",
    "@nestjs/common": "11.0.0",
    "@nestjs/core": "11.0.0",
    "bullmq": "5.20.1",
    "ioredis": "5.4.1",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.1",
    "pino": "9.5.0",
    "nestjs-pino": "4.1.0"
  },
  "devDependencies": {
    "@nestjs/cli": "11.0.0",
    "@nestjs/testing": "11.0.0",
    "@types/node": "20.16.11",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
```

- [ ] **Step 2: Create `apps/worker/tsconfig.json`** (same shape as `apps/api/tsconfig.json` — CommonJS, decorator metadata)

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "target": "ES2022",
    "isolatedModules": false
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Create `apps/worker/nest-cli.json`** (same shape as api's)

```json
{ "$schema": "https://json.schemastore.org/nest-cli", "collection": "@nestjs/schematics", "sourceRoot": "src" }
```

- [ ] **Step 4: Create `apps/worker/src/heartbeat/heartbeat.processor.ts`**

```ts
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Logger } from '@nestjs/common';
import type { Job } from 'bullmq';

export const HEARTBEAT_QUEUE = 'heartbeat';

@Processor(HEARTBEAT_QUEUE)
export class HeartbeatProcessor extends WorkerHost {
  private readonly logger = new Logger(HeartbeatProcessor.name);
  static counter = 0;

  async process(_job: Job): Promise<{ counter: number; ts: string }> {
    HeartbeatProcessor.counter += 1;
    const payload = { counter: HeartbeatProcessor.counter, ts: new Date().toISOString() };
    this.logger.log(`heartbeat tick ${JSON.stringify(payload)}`);
    return payload;
  }
}
```

- [ ] **Step 5: Create `apps/worker/src/heartbeat/heartbeat.scheduler.ts`**

```ts
import { Injectable, OnModuleInit, Inject } from '@nestjs/common';
import { InjectQueue } from '@nestjs/bullmq';
import type { Queue } from 'bullmq';
import { HEARTBEAT_QUEUE } from './heartbeat.processor.js';

@Injectable()
export class HeartbeatScheduler implements OnModuleInit {
  constructor(@InjectQueue(HEARTBEAT_QUEUE) private readonly q: Queue) {}

  async onModuleInit() {
    // Repeatable job every 60s
    await this.q.add(
      'tick',
      {},
      {
        repeat: { every: 60_000 },
        removeOnComplete: 100,
        removeOnFail: 100,
      },
    );
  }
}
```

- [ ] **Step 6: Create `apps/worker/src/heartbeat/heartbeat.module.ts`**

```ts
import { Module } from '@nestjs/common';
import { BullModule } from '@nestjs/bullmq';
import { HeartbeatProcessor, HEARTBEAT_QUEUE } from './heartbeat.processor.js';
import { HeartbeatScheduler } from './heartbeat.scheduler.js';

@Module({
  imports: [BullModule.registerQueue({ name: HEARTBEAT_QUEUE })],
  providers: [HeartbeatProcessor, HeartbeatScheduler],
})
export class HeartbeatModule {}
```

- [ ] **Step 7: Create `apps/worker/src/app.module.ts`**

```ts
import { Module } from '@nestjs/common';
import { BullModule } from '@nestjs/bullmq';
import { LoggerModule } from 'nestjs-pino';
import { HeartbeatModule } from './heartbeat/heartbeat.module.js';

@Module({
  imports: [
    LoggerModule.forRoot({
      pinoHttp: {
        transport: process.env.NODE_ENV === 'production' ? undefined : { target: 'pino-pretty' },
      },
    }),
    BullModule.forRoot({
      connection: {
        url: process.env.REDIS_URL ?? 'redis://localhost:6379',
      },
    }),
    HeartbeatModule,
  ],
})
export class AppModule {}
```

- [ ] **Step 8: Create `apps/worker/src/main.ts`**

```ts
import 'reflect-metadata';
import { NestFactory } from '@nestjs/core';
import { Logger } from 'nestjs-pino';
import { parseEnv } from '@holo/env';
import { AppModule } from './app.module.js';

async function bootstrap() {
  parseEnv(process.env);
  const app = await NestFactory.createApplicationContext(AppModule, { bufferLogs: true });
  app.useLogger(app.get(Logger));
  console.log('apps/worker started; heartbeat scheduled every 60s');
  await new Promise(() => {}); // run forever
}

bootstrap().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 9: Create `apps/worker/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({ test: { include: ['test/**/*.test.ts'] } });
```

- [ ] **Step 10: Write test `apps/worker/test/heartbeat.test.ts`** — unit test the processor's logic

```ts
import { describe, it, expect } from 'vitest';
import { HeartbeatProcessor } from '../src/heartbeat/heartbeat.processor.js';

describe('HeartbeatProcessor', () => {
  it('increments counter on each process call', async () => {
    const proc = new HeartbeatProcessor();
    const before = HeartbeatProcessor.counter;
    const r1 = await proc.process({} as never);
    const r2 = await proc.process({} as never);
    expect(r1.counter).toBe(before + 1);
    expect(r2.counter).toBe(before + 2);
    expect(typeof r1.ts).toBe('string');
  });
});
```

- [ ] **Step 11: Run tests**

```bash
pnpm install
pnpm -F @holo/worker test
```

Expected: 1 test passes.

- [ ] **Step 12: Smoke test against running Redis** (optional but recommended)

```bash
docker compose up -d redis
DATABASE_URL=postgresql://holo:holo@localhost:5432/holo \
  REDIS_URL=redis://localhost:6379 \
  HOLO_TOKEN_ENCRYPTION_KEY=$(openssl rand -base64 32) \
  BETTER_AUTH_SECRET=$(openssl rand -base64 32) \
  BETTER_AUTH_URL=http://localhost:3000 \
  GITHUB_LOGIN_CLIENT_ID=t GITHUB_LOGIN_CLIENT_SECRET=t \
  GITHUB_CONNECTOR_CLIENT_ID=t GITHUB_CONNECTOR_CLIENT_SECRET=t \
  pnpm -F @holo/worker dev &
sleep 75
docker compose exec redis redis-cli LLEN bull:heartbeat:completed
kill %1
```

Expected: at least 1 completed job in the queue (heartbeat ticked at least once).

- [ ] **Step 13: Commit**

```bash
git add apps/worker pnpm-lock.yaml
git commit -m "feat(worker): nestjs standalone with bullmq heartbeat queue (60s)"
```

---

## End of Part 4

After Part 4, the three server apps (api/mcp/worker) compile, test, and boot. Continue with [`2026-04-29-v0.0-foundation-implementation-part-5.md`](./2026-04-29-v0.0-foundation-implementation-part-5.md) for the Next.js dashboard with login + Connections page + GitHub OAuth flow — the largest single part.

**Verification before moving on:**

```bash
pnpm -F @holo/api test
DATABASE_URL=postgresql://holo:holo@localhost:5432/holo pnpm -F @holo/mcp test
pnpm -F @holo/worker test
```

Expected: all green.
