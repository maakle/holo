// Skill labels deferred from the MVP. See README roadmap.
import { deferred } from '@/lib/feature-deferred';

export const GET = () => deferred('skill labels');
export const POST = () => deferred('skill labels');
